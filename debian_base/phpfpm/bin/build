#!/bin/bash -e

script_dir=$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )
image_name=$(basename $(dirname ${script_dir}))
full_image_name="docker.internal.vektor.nyc/core/${image_name}:latest"

docker build --rm=true -t ${full_image_name} .
